{
  "name": "zuggernaut_mvp",
  "version": "1.1.0",
  "private": true,
  "description": "Root workspace helpers (backend/frontend are separate packages)",
  "scripts": {
    "check:before-push": "node scripts/check-before-push.js",
    "test": "npm run test --prefix frontend && npm run test --prefix backend",
    "ci": "npm run ci --prefix frontend && npm run ci --prefix backend"
  }
}
