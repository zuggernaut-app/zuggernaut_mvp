## Copy to `.env` in this folder only if you need to pin image versions differently.
## Defaults are embedded in `docker-compose.yml`.

COMPOSE_PROJECT_NAME=zuggernaut-temporal
ELASTICSEARCH_VERSION=7.17.27
POSTGRESQL_VERSION=16
TEMPORAL_VERSION=1.29.1
TEMPORAL_UI_VERSION=2.34.0
